name: "Development"
slug: "dev"
description: "Debian with development tools (Node.js, Python, Git, Docker CLI)"
icon: "code"
version: "1.0.0"

resources:
  cpu_limit: 4.0
  memory_limit: "4g"
  disk_limit: "20g"
  gpu_enabled: false

options:
  persistent_home: true
  security_relaxed: false
  enabled: true
  is_default: false
  sort_order: 4
