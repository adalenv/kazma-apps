name: "Debian Desktop"
slug: "debian"
description: "Full-featured Debian 12 with XFCE desktop environment"
icon: "monitor"
version: "1.0.0"

resources:
  cpu_limit: 2.0
  memory_limit: "2g"
  disk_limit: "10g"
  gpu_enabled: false

options:
  persistent_home: true
  security_relaxed: false
  enabled: true
  is_default: true
  sort_order: 1
