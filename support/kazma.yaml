name: "Support Desktop"
slug: "support"
description: "Ubuntu with Chrome, AnyDesk, TeamViewer for remote support"
icon: "headphones"
version: "1.0.0"

resources:
  cpu_limit: 2.0
  memory_limit: "2g"
  disk_limit: "10g"
  gpu_enabled: false

options:
  persistent_home: true
  security_relaxed: true
  enabled: true
  is_default: false
  sort_order: 5
